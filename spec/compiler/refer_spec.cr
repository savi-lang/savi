describe Mare::Compiler::Refer do
  it "complains when a local declared in a choice is used in another branch" do
    pending "branched analysis of local declarations"
  end
  
  it "complains when a consumed local is referenced" do
    pending "branched analysis of local consumption"
  end
end
