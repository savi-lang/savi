:import "spec"

:actor Main
  :new (env)
    Specs(ArraySpec).run(env)
    Specs(BoolSpec).run(env)
    Specs(InspectSpec).run(env)
    Specs(NumericSpec).run(env)
    Specs(PlatformSpec).run(env)
    Specs(StringSpec).run(env)
