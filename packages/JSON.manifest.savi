:manifest lib JSON
  :sources "src/JSON/**/*.savi"

:manifest bin "spec-JSON"
  :copies JSON
  :sources "spec/JSON/**/*.savi"

  :dependency Spec v0
    :depends on Map

  :transitive dependency Map v0
