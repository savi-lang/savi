:manifest lib Regex
  :sources "src/Regex/**/*.savi"

:manifest bin "spec-Regex"
  :copies Regex
  :sources "spec/Regex/**/*.savi"

  :dependency Spec v0
    :depends on Map

  :transitive dependency Map v0
