:import "spec"

:actor Main
  :new (env)
    Specs.run([
      SpecRunner(Day1Spec).new(env)
      SpecRunner(Day2Spec).new(env)
      SpecRunner(Day3Spec).new(env)
      SpecRunner(Day4Spec).new(env)
      SpecRunner(Day5Spec).new(env)
    ])
