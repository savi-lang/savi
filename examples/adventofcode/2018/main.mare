:import "spec"

:actor Main
  :new (env)
    Specs.run(env, [
      SpecRun(Day1Spec).new(env)
      SpecRun(Day2Spec).new(env)
      SpecRun(Day3Spec).new(env)
      SpecRun(Day4Spec).new(env)
      SpecRun(Day5Spec).new(env)
    ])
