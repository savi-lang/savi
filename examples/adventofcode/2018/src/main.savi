:actor Main
  :new (env)
    Spec.Process.run(env, [
      Spec.Run(Day1Spec).new(env)
      Spec.Run(Day2Spec).new(env)
      Spec.Run(Day3Spec).new(env)
      Spec.Run(Day4Spec).new(env)
      Spec.Run(Day5Spec).new(env)
    ])
